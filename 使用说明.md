# 个人博客网站 — 使用说明

---

## 一、项目概述

个人博客网站是一个基于 **C++ HTTP 服务器** + **MySQL 数据库** + **纯前端 SPA** 的个人博客管理系统。

- **后端**：C++17 编写的高性能 HTTP 服务器（Linux epoll / Windows IOCP），支持 RESTful API
- **前端**：纯原生 JavaScript SPA（无框架），支持 Markdown 编辑、实时预览、多主题切换
- **数据存储**：支持两种模式
  - **在线模式**：数据存储在 MySQL 数据库（`blogdb`）
  - **离线模式**：数据存储在浏览器 `localStorage`

---

## 二、环境要求

| 组件 | 要求 |
|------|------|
| 操作系统 | Windows 10/11 或 Linux |
| 浏览器 | Chrome / Edge / Firefox（推荐最新版） |
| MySQL | 5.7 或 8.0（默认端口 3306） |
| 编译器 | 仅构建需要：g++ 7+ / MSVC（已预编译 `blog_server.exe`） |

---

## 三、快速启动

### 3.1 启动 MySQL

确保 MySQL 服务已启动。Windows 下可通过 `start.bat` 自动启动。

### 3.2 启动服务器

```powershell
# 项目根目录下双击 start.bat，或手动运行：
server\blog_server.exe 8080
```

看到如下信息表示启动成功：

```
Blog Server v2.0 (Linux epoll + MySQL)
Static dir: D:\C++_Work\Project_10_PersonalBlogWebsite
DB: 127.0.0.1:3306/blogdb
MySQL connected to 127.0.0.1:3306/blogdb
Users in DB: 3
HTTP server started on port 8080 (16 worker threads)

============================================
  Blog server is running!
  Open http://127.0.0.1:8080
  Press Ctrl+C to stop
============================================
```

### 3.3 打开浏览器

访问 `http://127.0.0.1:8080/` 即可进入博客。

### 3.4 停止服务器

在终端窗口中按 **Ctrl+C** 即可优雅关闭服务器。

---

## 四、功能使用指南

### 4.1 浏览博客

| 页面 | 功能 |
|------|------|
| **首页** | 展示文章列表 + 个人信息（头像、名称、标语，同步自"关于"页面） |
| **写文章** | Markdown 编辑器，支持实时预览、文章管理 |
| **归档** | 按时间倒序查看文章 |
| **竞赛经历** | 展示竞赛获奖记录，支持编辑 |
| **项目经历** | 展示个人项目，支持编辑 |
| **工作经历** | 展示工作履历，支持编辑 |
| **关于** | 个人简介、头像、技能栈、联系方式，支持编辑 |
| **设置** | 主题切换、导航栏页面显隐控制 |

点击文章卡片可查看完整内容（弹窗显示）。

### 4.2 注册与登录

1. 点击右下角 **"登录"** 按钮（浮动在右下角）
2. 点击 **"注册账号"** 切换到注册模式
3. 输入：用户名、密码、昵称（选填）
4. 点击 **"注册"** 完成注册
5. 注册后自动登录，状态栏显示用户名

> **注意**：未登录时使用浏览器本地存储；登录后数据自动同步到服务器。

### 4.3 写文章

登录后：

1. 点击导航栏 **"写文章"**
2. 填写：标题、分类、标签（逗号分隔）
3. 在编辑区使用 **Markdown 语法** 写文章
4. 切换到 **"预览"** 标签查看效果
5. 点击 **"发布文章"** 保存
6. 快捷键：**Ctrl + S** 快速发布

**支持 Markdown 语法：**

```markdown
# 一级标题
## 二级标题
**粗体**
*斜体*
- 列表项
1. 有序列表
`行内代码`
```代码块```
> 引用
[链接](url)
![图片](url)
```

### 4.4 文章管理

在 **"写文章"** 页面下方 **"我的文章管理"** 区域：

- **编辑**：点击文章旁的"编辑"按钮，加载到编辑器修改
- **删除**：点击"删除"按钮，确认后删除

### 4.5 管理个人数据

#### 竞赛经历
1. 进入 **"竞赛经历"** 页面 → 点击 **"编辑"**
2. 添加/修改/删除记录 → 点击 **"保存"**

#### 项目经历
1. 进入 **"项目经历"** 页面 → 点击 **"编辑"**
2. 添加/修改/删除项目 → 点击 **"保存"**

#### 工作经历
1. 进入 **"工作经历"** 页面 → 点击 **"编辑"**
2. 添加/修改/删除工作记录 → 点击 **"保存"**

> 登录状态下，所有数据保存到服务器 MySQL；离线状态下保存在浏览器本地。

### 4.6 关于我编辑

"关于"页面支持编辑个人资料，编辑保存后**自动同步到首页**（头像、名称、标语）。

**可编辑字段：**

| 字段 | 说明 | 首页同步 |
|------|------|----------|
| 头像 URL | 头像图片链接，支持网络图片和本地图片 | 同步到首页头像 |
| 名称 | 你的名字 | 同步到首页"你好，我是 XXX" |
| 岗位 / 标语 | 如"全栈开发者" | 同步到首页副标题 |
| 简介 | 支持 HTML 格式 | 同步到首页描述（纯文本） |
| 技能栈 | 输入后点击"添加"，点击标签 ✕ 可删除 | — |
| 邮箱 / GitHub / 知乎 | 联系方式 | — |

**头像设置方式：**
- **网络图片**：右键复制网络图片链接，粘贴到头像 URL 字段
- **本地图片**：将图片放入项目 `images/` 目录，URL 填写 `/images/你的文件名.jpg`

### 4.7 页面设置

1. 进入 **"设置"** 页面
2. 勾选/取消勾选想要显示/隐藏的导航栏页面
3. 点击 **"保存设置"**
4. 设置实时生效（首页、关于、设置页面强制显示）

---

## 五、数据存储说明

### 在线模式

登录后，所有数据存储在 MySQL 数据库 `blogdb` 中，包含 7 张表：

| 表名 | 存储内容 |
|------|----------|
| `users` | 用户账号信息 |
| `posts` | 文章数据 |
| `awards` | 竞赛经历 |
| `projects` | 项目经历 |
| `work_experience` | 工作经历 |
| `page_settings` | 页面设置与主题 |
| `about` | 关于我资料 |

### 离线模式

未登录时，数据存储在浏览器 `localStorage` 中：

| localStorage Key | 存储内容 |
|---|---|
| `blogPosts` | 文章数据 |
| `blogCurrentUser` | 当前登录用户 |
| `blogPageSettings` | 页面设置与主题 |
| `blogProfile_awards` | 竞赛经历 |
| `blogProfile_projects` | 项目经历 |
| `blogProfile_work` | 工作经历 |
| `blogProfile_about` | 关于我资料 |

> **清除浏览器缓存将导致离线数据丢失**，建议登录后使用在线模式。

---

## 六、API 接口文档

服务器提供以下 RESTful API 接口（全部返回 JSON）：

### 6.1 用户认证

**注册** `POST /api/register`

```json
// 请求
{ "username": "myuser", "password": "mypass", "nickname": "昵称" }
// 响应
{ "success": true, "message": "注册成功", "username": "myuser" }
```

**登录** `POST /api/login`

```json
// 请求
{ "username": "myuser", "password": "mypass" }
// 响应
{ "success": true, "message": "登录成功", "user": { "username": "myuser", "nickname": "昵称", "avatar": "" } }
```

### 6.2 文章管理

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/posts?username=` | GET | 获取文章列表 |
| `/api/posts` | POST | 发布文章 |
| `/api/posts?username=&id=` | DELETE | 删除文章 |

### 6.3 个人数据

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/awards?username=` | GET / POST | 获取 / 批量保存竞赛经历 |
| `/api/projects?username=` | GET / POST | 获取 / 批量保存项目经历 |
| `/api/work?username=` | GET / POST | 获取 / 批量保存工作经历 |
| `/api/about?username=` | GET / POST | 获取 / 保存关于我资料 |
| `/api/settings?username=` | GET / POST | 获取 / 保存页面设置 |

**关于我 API 请求体示例：**

```json
{
    "name": "小明",
    "tagline": "全栈开发者",
    "avatar": "https://example.com/avatar.jpg",
    "description": "个人简介...",
    "skills": ["JavaScript", "C++", "Python"],
    "email": "user@example.com",
    "github": "github.com/username",
    "zhihu": "zhihu.com/people/username"
}
```

---

## 七、自定义开发

### 修改端口

```powershell
server\blog_server.exe 3000
```

访问 `http://127.0.0.1:3000/`

### 修改 API 地址

`api-client.js` 顶部：

```javascript
const API_BASE = 'http://127.0.0.1:8080';
```

### 添加本地图片

将图片文件放入项目根目录的 `images/` 文件夹，然后在头像 URL 或文章中用 `/images/文件名.jpg` 引用。服务器支持 `.png`、`.jpg`、`.jpeg` 格式。

### 重新编译服务器

```bash
cd server && mkdir build && cd build
cmake .. && make -j$(nproc)
```

---

## 八、常见问题

### Q: 启动时端口被占用

**解决**：更换端口号，如 `blog_server.exe 3000`

### Q: 页面显示异常

**解决**：按 **Ctrl+F5** 强制刷新，清除浏览器缓存。

### Q: 登录后数据为空

**说明**：新注册的账号服务器上没有数据，这是正常现象。登录后添加数据即可。

### Q: 头像不显示

**检查**：
- 网络图片：确认链接可以正常访问
- 本地图片：确认文件放在 `images/` 目录下，URL 以 `/images/` 开头

### Q: 如何重置所有数据

**清理浏览器缓存**：开发者工具 → Application → Clear storage → Clear site data

---

## 九、技术架构

```
+--------------------------------------------------+
|                    浏览器 (SPA)                     |
|  +----------+ +---------+ +-------------------+  |
|  |index.html | |app.js   | | profile-data.js   |  |
|  |页面模板   | |交互逻辑 | | 档案数据管理       |  |
|  +----------+ +---------+ +-------------------+  |
|  +----------+ +---------+ +-------------------+  |
|  |data.js   | |api-cli  | |online-integration |  |
|  |文章数据   | |API 通信 | |在线/离线模式切换   |  |
|  +----------+ +---------+ +-------------------+  |
+--------------------+----------------------------+
                     | HTTP REST API (JSON)
                     v
+--------------------------------------------------+
|             C++ HTTP 服务器 (epoll + 线程池)        |
|  +-------------+ +----------------------------+   |
|  | httpserver  | | blog_server.cpp            |   |
|  | 事件驱动服务 | | 路由分发 & 业务逻辑         |   |
|  +-------------+ +----------------------------+   |
|  +----------------------------------------------+ |
|  |     blogdb.cpp / blogdb.h                     | |
|  |     MySQL C API 封装，线程安全                 | |
|  +----------------------------------------------+ |
+--------------------+----------------------------+
                     v
            +-------------------+
            |  MySQL (blogdb)   |
            |  7 张表           |
            +-------------------+
```

---

## 十、项目文件结构

```
Project_10_PersonalBlogWebsite/
+--- index.html              # 主页面（含所有页面模板）
+--- style.css               # 全局样式（CSS 变量体系）
+--- themes.css              # 5 套主题定义
+--- app.js                  # 核心交互逻辑
+--- data.js                 # 文章数据管理
+--- profile-data.js         # 个人档案数据管理
+--- api-client.js           # API 通信客户端
+--- online-integration.js   # 在线/离线模式切换
+--- marked.min.js           # Markdown 解析库
+--- images/                 # 本地图片目录
+--- 使用说明.md              # 本说明文档
|
+--- server/                 # C++ 后端服务器
|   +--- blog_server.cpp     # 主程序 & 路由
|   +--- httpserver.cpp      # HTTP 服务器实现
|   +--- httpserver.h        # HTTP 服务器头文件
|   +--- blogdb.cpp          # 数据库管理实现
|   +--- blogdb.h            # 数据库管理头文件
|   +--- CMakeLists.txt      # CMake 构建配置
|   +--- blog_server.exe     # 已编译的可执行文件
|
+--- docs/                   # 技术文档
|   +--- architecture.md     # 系统架构文档
|   +--- technical.md        # 技术实现文档
|   +--- user-manual.md      # 英文版使用手册
|
+--- start.bat               # 一键启动脚本
```

---

## 版本信息

- **版本**：2.0
- **后端**：C++17 + Linux epoll + MySQL
- **前端**：纯原生 JavaScript SPA（ES6）
- **数据库**：MySQL 5.7+，7 张表
- **协议**：HTTP/1.1
- **端口**：8080（默认，可配置）
